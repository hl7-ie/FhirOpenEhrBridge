apiVersion: v1
kind: Service
metadata:
  name: bridge-api-java
  labels:
    app.kubernetes.io/name: bridge-api-java
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bridge-api-java
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
