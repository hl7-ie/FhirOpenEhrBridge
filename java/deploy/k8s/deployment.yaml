apiVersion: apps/v1
kind: Deployment
metadata:
  name: bridge-api-java
  labels:
    app.kubernetes.io/name: bridge-api-java
    app.kubernetes.io/part-of: fhir-openehr-bridge
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: bridge-api-java
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bridge-api-java
        app.kubernetes.io/part-of: fhir-openehr-bridge
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bridge-api
          image: ghcr.io/hl7-ie/fhiropenehrbridge-java:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          resources:
            requests:
              cpu: "100m"
              memory: "192Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
