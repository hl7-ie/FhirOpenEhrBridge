apiVersion: v1
kind: Service
metadata:
  name: bridge-api-nodejs
  labels:
    app.kubernetes.io/name: bridge-api-nodejs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bridge-api-nodejs
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
