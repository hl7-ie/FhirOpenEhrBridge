apiVersion: v1
kind: Service
metadata:
  name: bridge-api-python
  labels:
    app.kubernetes.io/name: bridge-api-python
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bridge-api-python
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
