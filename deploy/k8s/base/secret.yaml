# Placeholder Secret for CDR credentials. In real environments do NOT commit
# real secrets — inject them via Sealed Secrets, External Secrets Operator, or
# your platform's secret manager. ArgoCD users typically ignore this file and
# manage the Secret out-of-band.
apiVersion: v1
kind: Secret
metadata:
  name: bridge-api-secret
  labels:
    app.kubernetes.io/name: bridge-api
type: Opaque
stringData:
  OpenEhrCdr__Username: "ehrbase-user"
  OpenEhrCdr__Password: "CHANGE_ME"
