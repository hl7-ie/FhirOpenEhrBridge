apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bridge-api
spec:
  rules:
    - host: bridge-dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bridge-api
                port:
                  name: http
