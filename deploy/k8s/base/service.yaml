apiVersion: v1
kind: Service
metadata:
  name: bridge-api
  labels:
    app.kubernetes.io/name: bridge-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bridge-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
