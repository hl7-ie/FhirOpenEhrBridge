apiVersion: v1
kind: Service
metadata:
  name: bridge-api-rust
  labels:
    app.kubernetes.io/name: bridge-api-rust
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bridge-api-rust
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
